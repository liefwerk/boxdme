TMDB_API_KEY=your_tmdb_api_key_here

# Only this Letterboxd username is saved to boxdme.sqlite3. Leave empty for session-only (no disk watchlists).
PERSIST_USERNAME=liefwerk

# Remove persisted owner data and in-memory guest sessions after this many hours without use.
WATCHLIST_RETENTION_HOURS=24
